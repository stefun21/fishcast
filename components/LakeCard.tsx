import Link from 'next/link';import {MapPin,Star,Clock,Navigation} from 'lucide-react';import {Lake} from '@/lib/types';import FavoriteButton from './FavoriteButton';
export default function LakeCard({lake}:{lake:Lake}){return <Link href={`/balta/${lake.id}`} className="lake-card"><div className="lake-image" style={{backgroundImage:`url(${lake.image})`}}><div className="card-fav"><FavoriteButton id={lake.id} compact/></div>{lake.distance!==undefined&&<span className="distance-chip">{lake.distance.toFixed(1)} km</span>}</div><div className="lake-body"><div className="lake-title-row"><div><h3>{lake.name}</h3><p><MapPin size={15}/>{lake.locality}, {lake.county}</p></div><span className="rating" title="Scor FishCast"><Star size={15} fill="currentColor"/>{lake.rating}</span></div><div className="fish-tags">{lake.fish.slice(0,4).map(f=><span key={f}>{f}</span>)}</div><div className="card-meta"><span><Clock size={15}/>{lake.hours}</span><span><Navigation size={15}/>{lake.price}</span></div></div></Link>}
